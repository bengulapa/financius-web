import { EnumSeparatorPipe } from './enum-separator.pipe';

describe('EnumSeparatorPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumSeparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
